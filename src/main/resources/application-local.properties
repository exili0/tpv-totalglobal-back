# Perfil local para desarrollo.
# Arranque recomendado:
#   mvnw spring-boot:run -Dspring-boot.run.profiles=local

spring.datasource.url=jdbc:mysql://localhost:3306/tpv_totalglobal_db?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=XYDSNVY94F82EJn?
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Secret local de desarrollo.
# En entorno real usa JWT_SECRET como variable de entorno.
app.jwt.secret=TPVTotalGlobalSuperSecretJwtKeyFor2026DefenseMustBeLongEnough
app.jwt.expiration-ms=28800000
